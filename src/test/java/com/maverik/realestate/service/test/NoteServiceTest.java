package com.maverik.realestate.service.test;


//@RunWith(MockitoJUnitRunner.class)
//@FixMethodOrder(MethodSorters.NAME_ASCENDING)
public class NoteServiceTest {
    /*
     * @Mock private PropertyNotesRepository noteReferenceRepositoryMock;
     * 
     * @InjectMocks private NoteManagementServiceImpl noteManagementService;
     * 
     * @Test public void testFindPropertyNotes() throws GenericException {
     * Property property = new Property(); property.setId(1L);
     * List<PropertyNotes> entities = new ArrayList<PropertyNotes>(); Note note
     * = new Note(); note.setNoteId(1L); note.setNotes("Testing note");
     * PropertyNotes noteReference = new PropertyNotes();
     * noteReference.setNoteId(note); noteReference.setPropertyId(property);
     * User user = new User(); user.setId(1L); user.setFirstName("Jorge");
     * user.setLastName("Mendoza"); noteReference.setUserId(user);
     * note.setNotesReferences(noteReference); entities.add(noteReference);
     * when(
     * noteReferenceRepositoryMock.findByPropertyId(property)).thenReturn(entities
     * ); Assert.assertNotNull(noteManagementService); List<NoteBean> notes =
     * noteManagementService.findNotesBy("Property", 1L);
     * Assert.assertNotNull(notes); Assert.assertEquals("Jorge Mendoza",
     * notes.get(0).getUser()); Assert.assertEquals(Long.valueOf(1L),
     * notes.get(0).getNoteId()); Assert.assertEquals("Testing note",
     * notes.get(0).getNotes()); }
     * 
     * @Test public void testFailFindPropertyNotes() throws GenericException {
     * Property property = new Property(); property.setId(1L);
     * List<PropertyNotes> entities = new ArrayList<PropertyNotes>(); Note note
     * = new Note(); note.setNoteId(1L); note.setNotes("Testing note");
     * PropertyNotes noteReference = new PropertyNotes();
     * noteReference.setNoteId(note); noteReference.setPropertyId(property);
     * User user = new User(); user.setId(1L); user.setFirstName("Homer");
     * user.setLastName("Simpson"); noteReference.setUserId(user);
     * note.setNotesReferences(noteReference); entities.add(noteReference);
     * when(
     * noteReferenceRepositoryMock.findByPropertyId(property)).thenReturn(entities
     * ); Assert.assertNotNull(noteManagementService); List<NoteBean> notes =
     * noteManagementService.findNotesBy("Property", 1L);
     * Assert.assertNotNull(notes); Assert.assertNotEquals("Jorge Mendoza",
     * notes.get(0).getUser()); Assert.assertNotEquals(Long.valueOf(2L),
     * notes.get(0).getNoteId()); Assert.assertNotEquals("Testing notes",
     * notes.get(0).getNotes()); }
     */
}
